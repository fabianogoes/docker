# Docker Compose to Postgres with Adminer

## Running

> UP 

```shell
docker-compose -f postgres-docker-compose.yaml up -d
```

> Down

```shell
docker-compose -f postgres-docker-compose.yaml down
```

## Adminer

http://localhost:8080

Sistema: `PostgreSQL`   
Servidor: `postgres`   
Usuário: `user`   
Senha: `pass`   
Database: `dev`   

